name: deploy frontend release applications

on:
  release:
    types:
      - published
  workflow_call:
    inputs:
      source_sha:
        description: Fixed main source SHA to deploy
        required: true
        type: string
      release_key:
        description: Release key in YYYY-MM-DD/N format
        required: true
        type: string
      release_type:
        description: Release PR type (standard or hotfix)
        required: false
        default: standard
        type: string
      candidate_sha:
        description: Reviewed PR head SHA
        required: false
        default: ''
        type: string
      base_source_sha:
        description: Previously deployed Release PR source for a hotfix
        required: false
        default: ''
        type: string

permissions:
  contents: read
  actions: read

concurrency:
  group: deploy-frontend-production-${{ github.event.release.tag_name || inputs.release_key }}
  cancel-in-progress: false

env:
  IMAGE_NAME: bottlenote-frontend

jobs:
  release-gate:
    name: gate frontend release
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      release-tag: ${{ steps.release.outputs.release-tag }}
      release-key: ${{ steps.release.outputs.release-key }}
      image-tag: ${{ steps.release.outputs.image-tag }}
      source-sha: ${{ steps.release.outputs.source-sha }}
      source-ref: ${{ steps.release.outputs.source-ref }}
      deployment-source: ${{ steps.release.outputs.deployment-source }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: validate frontend release
        id: release
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          SOURCE_SHA_INPUT: ${{ inputs.source_sha }}
          RELEASE_KEY_INPUT: ${{ inputs.release_key }}
          RELEASE_TYPE_INPUT: ${{ inputs.release_type }}
          CANDIDATE_SHA_INPUT: ${{ inputs.candidate_sha }}
          BASE_SOURCE_SHA_INPUT: ${{ inputs.base_source_sha }}
          GITHUB_EVENT_PATH: ${{ github.event_path }}
        run: |
          set -euo pipefail
          git fetch --no-tags origin '+refs/heads/main:refs/remotes/origin/main'

          if [[ "$GITHUB_EVENT_NAME" == "release" ]]; then
            [[ "$RELEASE_TAG" =~ ^frontend/v[0-9]+\.[0-9]+\.[0-9]+$ ]] || {
              echo "::error::frontend release tag required, got: $RELEASE_TAG"
              exit 1
            }
            version="${RELEASE_TAG#frontend/v}"
            release_key="$version"
            image_tag="frontend_${version}"
            deployment_source="github-release"
            source_ref="$RELEASE_TAG"

            git fetch --tags origin
            git rev-parse -q --verify "refs/tags/${RELEASE_TAG}" >/dev/null || {
              echo "::error::release tag not found: $RELEASE_TAG"
              exit 1
            }
            source_sha="$(git rev-list -n 1 "$RELEASE_TAG")"
            git merge-base --is-ancestor "$source_sha" origin/main || {
              echo "::error::tag commit is not contained in origin/main: $source_sha"
              exit 1
            }

            RELEASE_TAG="$RELEASE_TAG" GITHUB_EVENT_PATH="$GITHUB_EVENT_PATH" python3 - <<'PY'
          import json
          import os
          import sys

          with open(os.environ['GITHUB_EVENT_PATH'], encoding='utf-8') as event_file:
              release = json.load(event_file).get('release') or {}
          if release.get('tag_name') != os.environ['RELEASE_TAG']:
              print('::error::release payload tag mismatch', file=sys.stderr)
              sys.exit(1)
          if release.get('draft') or release.get('prerelease'):
              print('::error::release must be a published production release', file=sys.stderr)
              sys.exit(1)
          if len((release.get('body') or '').strip()) < 80:
              print('::error::release changelog is empty or too short', file=sys.stderr)
              sys.exit(1)
          PY
          else
            [[ "$SOURCE_SHA_INPUT" =~ ^[0-9a-f]{40}$ ]] || {
              echo "::error::source_sha must be a full commit SHA"
              exit 1
            }
            [[ "$RELEASE_KEY_INPUT" =~ ^([0-9]{4}-[0-9]{2}-[0-9]{2})/([1-9][0-9]*)$ ]] || {
              echo "::error::release_key must use YYYY-MM-DD/N format"
              exit 1
            }
            release_date="${BASH_REMATCH[1]}"
            sequence="${BASH_REMATCH[2]}"
            normalized_date="$(date -u -d "$release_date" +%F)" || {
              echo "::error::release_key contains an invalid calendar date"
              exit 1
            }
            [[ "$normalized_date" == "$release_date" ]] || {
              echo "::error::release_key contains an invalid calendar date"
              exit 1
            }

            source_sha="$SOURCE_SHA_INPUT"
            release_key="$RELEASE_KEY_INPUT"
            release_type="${RELEASE_TYPE_INPUT:-standard}"
            image_version="${release_date//-/.}.${sequence}"
            image_tag="frontend_${image_version}"
            deployment_source="release-pr"
            RELEASE_TAG=""

            git cat-file -e "${source_sha}^{commit}"
            case "$release_type" in
              standard)
                [[ -z "$BASE_SOURCE_SHA_INPUT" ]] || {
                  echo "::error::standard release must not provide base_source_sha"
                  exit 1
                }
                git merge-base --is-ancestor "$source_sha" origin/main || {
                  echo "::error::standard source_sha is not contained in origin/main: $source_sha"
                  exit 1
                }
                source_ref="main"
                ;;
              hotfix)
                [[ "$CANDIDATE_SHA_INPUT" =~ ^[0-9a-f]{40}$ && "$BASE_SOURCE_SHA_INPUT" =~ ^[0-9a-f]{40}$ ]] || {
                  echo "::error::hotfix release requires candidate_sha and base_source_sha"
                  exit 1
                }
                git cat-file -e "${CANDIDATE_SHA_INPUT}^{commit}"
                git cat-file -e "${BASE_SOURCE_SHA_INPUT}^{commit}"
                read -r merge_sha first_parent second_parent extra_parent <<< "$(git rev-list --parents -n 1 "$source_sha")"
                [[ "$merge_sha" == "$source_sha" && "$first_parent" == "$BASE_SOURCE_SHA_INPUT" && \
                   "$second_parent" == "$CANDIDATE_SHA_INPUT" && -z "${extra_parent:-}" ]] || {
                  echo "::error::hotfix deployment source is not the verified base-plus-candidate merge"
                  exit 1
                }
                source_ref="hotfixes/${release_key}"
                ;;
              *)
                echo "::error::release_type must be standard or hotfix"
                exit 1
                ;;
            esac
          fi

          {
            echo "release-tag=$RELEASE_TAG"
            echo "release-key=$release_key"
            echo "image-tag=$image_tag"
            echo "source-sha=$source_sha"
            echo "source-ref=$source_ref"
            echo "deployment-source=$deployment_source"
          } >> "$GITHUB_OUTPUT"

          {
            echo "## Frontend production release gate"
            echo "- Release key: \`${release_key}\`"
            echo "- Source: \`${deployment_source}\`"
            [[ "$deployment_source" != "release-pr" ]] || echo "- Release type: \`${release_type}\`"
            echo "- Image tag: \`${image_tag}\`"
            echo "- Source SHA: \`${source_sha}\`"
            echo "- Source ref: \`${source_ref}\`"
          } >> "$GITHUB_STEP_SUMMARY"

  build-frontend-image:
    name: build and push frontend image
    needs: release-gate
    runs-on: ubuntu-24.04-arm
    outputs:
      image-digest: ${{ steps.build.outputs.image-digest }}
      promoted-tags: ${{ steps.build.outputs.promoted-tags }}
      previous-digest: ${{ steps.build.outputs.previous-digest }}
    steps:
      - name: checkout release source with submodules
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.release-gate.outputs.source-sha }}
          submodules: true
          token: ${{ secrets.GIT_ACCESS_TOKEN }}

      # 릴리즈 소스(특히 핫픽스)는 과거 커밋이라 구 action 스냅샷을 들고 온다.
      # 원자화된 action 이 항상 쓰이도록 main 의 action 디렉터리로 덮어쓴다.
      - name: refresh docker build action from main
        run: |
          set -euo pipefail
          git fetch --no-tags origin '+refs/heads/main:refs/remotes/origin/main'
          git checkout origin/main -- .github/actions/docker-build-push

      - name: build and push docker image
        id: build
        uses: ./.github/actions/docker-build-push
        with:
          registry-url: ${{ secrets.REGISTRY_ADDRESS }}
          registry-username: ${{ secrets.REGISTRY_USERNAME }}
          registry-password: ${{ secrets.REGISTRY_PASSWORD }}
          image-name: ${{ env.IMAGE_NAME }}
          image-tag: ${{ needs.release-gate.outputs.image-tag }}
          promote-tags: ${{ needs.release-gate.outputs.deployment-source == 'release-pr' && 'frontend_latest_production' || '' }}
          source-sha: ${{ needs.release-gate.outputs.source-sha }}
          source-ref: ${{ needs.release-gate.outputs.source-ref }}
          cache-scope: frontend-production
          cache-export: 'false'
          sops-age-key: ${{ secrets.SOPS_AGE_SECRET_KEY }}
          env-file: git.environment-variables/application.next-js/prod.sops.env
          sign-image: 'true'
          cosign-key-path: git.environment-variables/storage/docker-registry/cosign.key
          cosign-public-key-path: git.environment-variables/storage/docker-registry/cosign.pub
          cosign-password: ${{ secrets.COSIGN_PASSWORD }}

  handoff-to-image-updater:
    name: hand off frontend image to image updater
    if: needs.release-gate.outputs.deployment-source == 'release-pr'
    needs:
      - release-gate
      - build-frontend-image
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      CHANNEL_TAG: frontend_latest_production
    steps:
      - name: login to private registry
        uses: docker/login-action@v4
        with:
          registry: ${{ secrets.REGISTRY_ADDRESS }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      # 승격된 채널 태그가 build job 이 서명·검증한 digest 를 가리키는지 독립적으로 재확인한다.
      - name: verify promoted channel digest
        env:
          IMAGE: ${{ secrets.REGISTRY_ADDRESS }}/${{ env.IMAGE_NAME }}
          EXPECTED_DIGEST: ${{ needs.build-frontend-image.outputs.image-digest }}
          PROMOTED_TAGS: ${{ needs.build-frontend-image.outputs.promoted-tags }}
          PREVIOUS_DIGEST: ${{ needs.build-frontend-image.outputs.previous-digest }}
          RELEASE_KEY: ${{ needs.release-gate.outputs.release-key }}
          SOURCE_SHA: ${{ needs.release-gate.outputs.source-sha }}
        run: |
          set -euo pipefail
          [[ "$EXPECTED_DIGEST" =~ ^sha256:[0-9a-f]{64}$ ]] || {
            echo "::error::build job did not report a valid image digest: ${EXPECTED_DIGEST}"
            exit 1
          }
          [[ "$PROMOTED_TAGS" == "$CHANNEL_TAG" ]] || {
            echo "::error::build job promoted '${PROMOTED_TAGS}', expected '${CHANNEL_TAG}'"
            exit 1
          }
          channel_digest="$(docker buildx imagetools inspect "${IMAGE}:${CHANNEL_TAG}" --format '{{json .Manifest.Digest}}' | tr -d '"')"
          [[ "$channel_digest" == "$EXPECTED_DIGEST" ]] || {
            echo "::error::${CHANNEL_TAG} points to ${channel_digest}, expected ${EXPECTED_DIGEST}"
            exit 1
          }
          {
            echo "## Frontend image updater handoff"
            echo "- Release key: \`${RELEASE_KEY}\`"
            echo "- Source SHA: \`${SOURCE_SHA}\`"
            echo "- Channel: \`${CHANNEL_TAG}\`"
            echo "- Digest: \`${EXPECTED_DIGEST}\`"
            echo "- Previous digest: \`${PREVIOUS_DIGEST:-none}\`"
            echo "- Channel digest re-check: \`passed\`"
            echo "- GitOps update: \`Argo CD Image Updater\`"
          } >> "$GITHUB_STEP_SUMMARY"

  update-production:
    name: update frontend production image
    if: github.event_name == 'release'
    needs:
      - release-gate
      - build-frontend-image
    runs-on: ubuntu-latest
    steps:
      - name: checkout code with submodules
        uses: actions/checkout@v7
        with:
          submodules: true
          token: ${{ secrets.GIT_ACCESS_TOKEN }}

      - name: setup kustomize
        uses: imranismail/setup-kustomize@v3

      - name: update production image tag
        env:
          REGISTRY: ${{ secrets.REGISTRY_ADDRESS }}
          IMAGE_TAG: ${{ needs.release-gate.outputs.image-tag }}
        run: |
          cd git.environment-variables
          git checkout main
          cd deploy/overlays/production
          kustomize edit set image "bottlenote-frontend=${REGISTRY}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}"

      - name: commit and push with retry
        run: |
          cd git.environment-variables
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add deploy/overlays/production/kustomization.yaml
          git diff --staged --quiet && exit 0
          git commit -m "chore(frontend): update production image tag to ${{ needs.release-gate.outputs.image-tag }}"
          for i in {1..5}; do
            git pull --rebase origin main && git push origin main && exit 0
            sleep $((i * 2))
          done
          exit 1
