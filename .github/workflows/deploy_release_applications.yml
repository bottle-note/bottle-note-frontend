name: deploy frontend release applications

on:
  release:
    types:
      - published

permissions:
  contents: read
  actions: read

concurrency:
  group: deploy-frontend-production-${{ github.event.release.tag_name }}
  cancel-in-progress: true

env:
  IMAGE_NAME: bottlenote-frontend

jobs:
  release-gate:
    name: gate frontend release
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      release-tag: ${{ steps.release.outputs.release-tag }}
      version: ${{ steps.release.outputs.version }}
      image-tag: ${{ steps.release.outputs.image-tag }}
      tag-sha: ${{ steps.release.outputs.tag-sha }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: validate published frontend release
        id: release
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          GITHUB_EVENT_PATH: ${{ github.event_path }}
        run: |
          set -euo pipefail
          [[ -n "$RELEASE_TAG" ]] || { echo "::error::release tag is required"; exit 1; }
          [[ "$RELEASE_TAG" =~ ^frontend/v[0-9]+\.[0-9]+\.[0-9]+$ ]] || { echo "::error::frontend release tag required, got: $RELEASE_TAG"; exit 1; }

          version="${RELEASE_TAG#frontend/v}"
          image_tag="frontend_${version}"

          git fetch --no-tags origin '+refs/heads/main:refs/remotes/origin/main'
          git fetch --tags origin
          git rev-parse -q --verify "refs/tags/${RELEASE_TAG}" >/dev/null || { echo "::error::release tag not found: $RELEASE_TAG"; exit 1; }
          tag_sha="$(git rev-list -n 1 "$RELEASE_TAG")"
          git merge-base --is-ancestor "$tag_sha" origin/main || { echo "::error::tag commit is not contained in origin/main: $tag_sha"; exit 1; }

          python3 - <<'PY'
          import json
          import os
          import sys

          event_path = os.environ['GITHUB_EVENT_PATH']
          release_tag = os.environ['RELEASE_TAG']
          with open(event_path, encoding='utf-8') as event_file:
              event = json.load(event_file)
          release = event.get('release') or {}

          if release.get('tag_name') != release_tag:
              print(f"::error::release payload tag mismatch: {release.get('tag_name')} != {release_tag}", file=sys.stderr)
              sys.exit(1)
          if release.get('draft'):
              print('::error::release must be published before production deploy', file=sys.stderr)
              sys.exit(1)
          if release.get('prerelease'):
              print('::error::production release must not be prerelease', file=sys.stderr)
              sys.exit(1)
          body = (release.get('body') or '').strip()
          if len(body) < 80:
              print('::error::release changelog is empty or too short', file=sys.stderr)
              sys.exit(1)
          print(f"release_id={release.get('id')} body_length={len(body)}")
          PY

          {
            echo "release-tag=$RELEASE_TAG"
            echo "version=$version"
            echo "image-tag=$image_tag"
            echo "tag-sha=$tag_sha"
          } >> "$GITHUB_OUTPUT"

          {
            echo "## Frontend Production Release Gate"
            echo "- Release tag: \`${RELEASE_TAG}\`"
            echo "- Version: \`${version}\`"
            echo "- Image tag: \`${image_tag}\`"
            echo "- Tag commit: \`${tag_sha}\`"
            echo "- Main ancestry: \`passed\`"
            echo "- Release metadata/changelog: \`passed\`"
          } >> "$GITHUB_STEP_SUMMARY"

          echo "[frontend-release-gate] passed release_tag=$RELEASE_TAG version=$version image_tag=$image_tag tag_sha=$tag_sha"

  build-frontend-image:
    name: build and push frontend image
    needs: release-gate
    runs-on: ubuntu-24.04-arm
    outputs:
      image-digest: ${{ steps.build.outputs.image-digest }}
    steps:
      - name: checkout release tag with submodules
        uses: actions/checkout@v7
        with:
          ref: ${{ needs.release-gate.outputs.release-tag }}
          submodules: true
          token: ${{ secrets.GIT_ACCESS_TOKEN }}

      - name: build and push docker image
        id: build
        uses: ./.github/actions/docker-build-push
        with:
          registry-url: ${{ secrets.REGISTRY_ADDRESS }}
          registry-username: ${{ secrets.REGISTRY_USERNAME }}
          registry-password: ${{ secrets.REGISTRY_PASSWORD }}
          image-name: ${{ env.IMAGE_NAME }}
          image-tag: ${{ needs.release-gate.outputs.image-tag }}
          cache-scope: frontend-production
          sops-age-key: ${{ secrets.SOPS_AGE_SECRET_KEY }}
          env-file: git.environment-variables/application.next-js/prod.sops.env
          sign-image: 'true'
          cosign-key-path: git.environment-variables/storage/docker-registry/cosign.key
          cosign-password: ${{ secrets.COSIGN_PASSWORD }}

  update-production:
    name: update frontend production image
    needs:
      - release-gate
      - build-frontend-image
    runs-on: ubuntu-latest
    steps:
      - name: checkout code with submodules
        uses: actions/checkout@v7
        with:
          submodules: true
          token: ${{ secrets.GIT_ACCESS_TOKEN }}

      - name: setup kustomize
        uses: imranismail/setup-kustomize@v3

      - name: update production image tag
        env:
          REGISTRY: ${{ secrets.REGISTRY_ADDRESS }}
          IMAGE_TAG: ${{ needs.release-gate.outputs.image-tag }}
        run: |
          cd git.environment-variables
          git checkout main

          cd deploy/overlays/production
          kustomize edit set image \
            bottlenote-frontend=${REGISTRY}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}

      - name: commit and push with retry
        env:
          RELEASE_TAG: ${{ needs.release-gate.outputs.release-tag }}
          IMAGE_TAG: ${{ needs.release-gate.outputs.image-tag }}
          TAG_SHA: ${{ needs.release-gate.outputs.tag-sha }}
        run: |
          cd git.environment-variables
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add deploy/overlays/production/kustomization.yaml

          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "chore(frontend): update production image tag to ${IMAGE_TAG}

          Updated by GitHub Actions
          Release: ${RELEASE_TAG}
          Commit: ${TAG_SHA}
          Frontend digest: ${{ needs.build-frontend-image.outputs.image-digest }}
          Workflow: ${{ github.workflow }}"

          for i in {1..5}; do
            git pull --rebase origin main && git push origin main && break
            echo "Push failed, retry $i/5..."
            sleep $((i * 2))
          done

          echo "Frontend production deployment triggered"
