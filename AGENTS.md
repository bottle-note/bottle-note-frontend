# Bottle Note Frontend Agent Guide

## 1. 탐색과 구현

- 기능에 필요한 route, API, 주변 코드만 확인하고 구조가 파악되면 구현을 시작한다.
- 기존 위치·명명·데이터 흐름을 따라 현재 요구사항을 만족하는 가장 단순하고 읽기 쉬운 코드를 작성한다.
- 범위 밖 리팩터링, 예상 재사용을 위한 추상화, 불필요한 helper·wrapper를 만들지 않는다.
- UI 변경은 `DESIGN.md`, `tailwind.config.ts`, 인접한 기존 컴포넌트를 확인하고 별도 요구가 없으면 기존 크기와 간격을 유지한다.
- 변경 기능이 로그인, 이미지 선택, 공유 또는 브라우저 API와 관련되면 일반 브라우저와 Flutter WebView의 동작 차이를 확인한다.

## 2. 컴포넌트와 상태

- 공용 컴포넌트와 같은 도메인의 기존 컴포넌트를 먼저 확인하고 같은 역할을 중복 구현하지 않는다.
- 화면 전용 컴포넌트는 기존 route의 `_components` 구조를 따르고, 실제로 여러 화면에서 재사용될 때만 `domain`, `feature`, `ui`의 현재 책임에 맞춰 이동한다.
- 서버 상태는 TanStack Query로 관리하고 Zustand에 복제하지 않는다. Zustand는 여러 화면이 공유하는 클라이언트 상태에만 사용하며 파생 가능한 상태를 별도로 저장하지 않는다.

## 3. API

- API 계약은 `https://docs.bottle-note.com/#product`를 기준으로 확인한다.
- 스펙에 없는 필드, 응답 래퍼, 기본값, 데이터 의미, 성공·오류 동작을 추측하지 않는다.
- 기존 domain API와 query 흐름을 따르고, 변경과 직접 관련 없는 API 계층을 함께 재구성하지 않는다.

## 4. 테스트와 검증

- 페이지·도메인 규모 기능은 구현 전 핵심 happy path를 제안하고 확인받는다.
- 승인된 시나리오는 Vitest + React Testing Library 통합 테스트로 먼저 실패시킨 뒤 Red → Green으로 구현한다.
- 작은 UI primitive, 단순 문구·스타일 변경에는 테스트를 강제하지 않는다.
- 추가 예외 테스트는 인증·권한, 저장·삭제, 데이터 변환, 조건별 API payload, 재현된 버그처럼 실패 시 사용자 데이터나 주요 흐름에 문제가 생기는 경우에만 작성한다.
- 브라우저 검증은 실제 입력·focus·스크롤·위치, 파일·공유·history·WebView 연동, 인증된 live 통합처럼 이번 변경의 정확성을 jsdom만으로 확인할 수 없거나 사용자가 요청한 경우에 수행한다.
- 필요한 검증이 Vitest/RTL로 충분하면 dropdown, modal 같은 UI 요소가 있다는 이유만으로 브라우저 검증을 추가하지 않는다.
