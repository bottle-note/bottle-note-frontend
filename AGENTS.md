# Bottle Note Frontend Agent Guide

## 1. 제품 맥락

- Bottle Note는 사용자가 주류를 탐색하고 별점·리뷰를 기록하며 취향을 공유하는 사용자용 프로덕트다. 현재 콘텐츠는 위스키 중심이며, 관리자용 화면의 작업 흐름이나 UI 기준을 그대로 적용하지 않는다.
- Next.js App Router, React, TypeScript, Tailwind CSS를 사용하며 패키지 매니저는 pnpm이다. 의존성 버전과 실행 명령은 `package.json`을 확인한다.
- 모바일 우선이며 일반 브라우저와 Flutter WebView 양쪽에서 동작해야 한다. 로그인, 이미지 선택, 공유, 뒤로 가기 등은 환경별 차이를 고려한다.
- 핵심 도메인은 주류 탐색, 별점, 리뷰, 찜(`pick`), 댓글, 팔로우, 마이보틀, 큐레이션, 테이스팅 이벤트·프로그램이다.
- 별점은 0.5점부터 5점까지 0.5점 단위다. 리뷰는 공개/비공개 상태와 병(`BOTTLE`)/잔(`GLASS`) 가격, 장소, 이미지, 테이스팅 태그를 포함할 수 있다. 마이보틀은 사용자의 별점·리뷰·찜 기록을 모아 보는 영역이다.

## 2. 작업 이해

- 구현 전에 사용자가 수행할 행동, 기대 결과, 완료 조건과 영향을 받는 기존 흐름을 파악한다. 제품 동작이 불명확하면 쟁점과 대안을 제시한다.
- 관련 route, 컴포넌트, API, 상태와 테스트를 확인해 기존 설계 의도를 이해한다. 탐색 범위는 작업에 필요한 만큼 정한다.
- 승인과 자율 진행 범위는 해당 세션의 사용자 지시를 따른다. 합의된 제품 의도와 완료 조건을 바탕으로 기술적인 구현·검증 방법을 판단한다.
- UI 작업은 이 문서에 이어 `DESIGN.md`, `tailwind.config.ts`, 인접한 기존 컴포넌트를 확인한다. 별도 요구가 없으면 기존 크기와 간격을 유지하며, 조정이 필요하면 이유와 화면 영향을 설명하고 검증한다.

## 3. 구현과 구조 개선

- 기존 구조와 설계 의도를 출발점으로 삼는다. 이번 작업과 연결된 책임 분리, 중복 제거, 상태와 데이터 흐름 개선은 구현에 포함할 수 있다.
- 변경량의 최소화보다 결과의 명확성과 안정성을 우선한다. 구조 개선은 어떤 복잡성을 줄이는지 설명할 수 있어야 하며, 작업과 무관한 재설계나 예상 재사용만을 위한 추상화는 피한다.
- 공용 컴포넌트와 같은 도메인의 기존 구현을 확인해 같은 역할을 중복 구현하지 않는다. 기존 패턴에 문제가 있으면 그대로 복제하지 않고 관련 사용처의 영향을 함께 확인한다.
- 함께 이해하고 변경하는 코드는 가까이 둔다. 책임이 다르거나 반복을 줄이고 흐름을 명확하게 만들 때 분리한다. 파일 길이, 컴포넌트 수, props 전달 단계만으로 분리 여부를 결정하지 않는다.
- 화면 전용 코드는 route의 `_components`에 두는 것을 출발점으로 삼는다. 실제 재사용 범위와 책임에 따라 도메인 로직은 `domain`, 도메인 독립 기능은 `feature`, 기본 UI 요소는 `ui`에 둔다.
- 입력, 변환, 출력이 드러나는 데이터 흐름을 유지한다. 사용자 변경을 보존하고 구조 개선에 따른 기존 사용처의 동작을 확인한다.

## 4. 데이터와 API

- 서버 상태는 TanStack Query로 관리하고 Zustand에 복제하지 않는다. 화면 내부 상태는 해당 화면 가까이 두고, Zustand는 여러 화면이 공유하는 클라이언트 상태에 사용한다. 계산 가능한 값을 별도 상태로 저장하지 않는다.
- API 작업 전 [Product API OpenAPI 문서](https://docs.bottle-note.com/#product)에서 경로, 메서드, 인증 여부와 요청·응답 스키마를 확인한다.
- 스펙에 없는 필드, 응답 래퍼, 기본값, 데이터 의미, 성공·오류 동작을 추측하지 않는다. 계약이 불명확하거나 실제 응답과 다르면 차이를 확인하고 드러낸다.
- 기존 domain API와 query 흐름을 확인하고, 변경에 필요한 데이터 변환과 캐시 갱신을 관련 사용처까지 일관되게 반영한다.

## 5. 테스트와 검증

- 도메인별 핵심 해피패스 시나리오는 사용자와 논의해 결정한다. 시나리오는 사용자 행동과 기대 결과로 표현하며, 합의된 시나리오가 있으면 재사용하고 변경되는 부분을 논의한다.
- 자동 테스트는 React Testing Library 컴포넌트 통합 테스트를 중심으로 작성한다. 관련 컴포넌트를 함께 렌더링하고 사용자 입력부터 화면 결과까지 확인한다.
- 현재 테스트 실행 도구는 Jest이며 Vitest 전환을 검토 중이다. 전환 작업 전까지는 저장소의 실제 설정을 사용하고, 일반 기능 작업에 도구 전환을 섞지 않는다. 테스트 작성 순서를 일률적으로 강제하지 않는다.
- 내부 함수 호출, 컴포넌트의 분리 방식, 상태 변수 같은 구현 세부사항에 테스트를 결합하지 않는다. API 응답 대역이 필요하면 실제 계약을 따르며, 이를 실제 서버 연동 검증으로 간주하지 않는다.
- 테스트 수나 커버리지보다 합의한 시나리오의 검증을 우선한다. 작은 UI 요소나 단순 문구·스타일 변경에 별도 자동 테스트를 강제하지 않는다.
- 예외 시나리오는 인증·권한, 저장·삭제, 데이터 변환, 요청 데이터 분기, 재현된 버그 등 실패 영향이 큰 부분에 필요한 만큼 추가한다. 버그 수정에는 재현 조건과 핵심 회귀 확인을 포함한다.
- 화면 구현 후에는 브라우저를 열어 합의한 시나리오의 입력·클릭·이동을 직접 수행하고 결과를 확인한다. 변경과 관련된 스크롤, 고정 영역, 키보드와 반응형 동작도 확인한다.
- 브라우저 조작 검증에는 Playwright MCP를 사용한다. 반복 실행할 Playwright E2E 테스트 코드의 작성·유지는 기본 작업에 포함하지 않는다.
- 실제 인증 흐름은 우회하거나 목킹하지 않는다. 필요한 경우 사용자에게 로그인을 요청하고 인증된 세션을 사용한다. WebView 전용 동작은 브라우저 검증과 구분한다.
- 사용자가 제시한 viewport와 문서 스크롤 높이가 있으면 같은 조건으로 확인한다. 무한 스크롤은 로딩 감지 요소 진입 전후의 네트워크 요청과 화면 결과를 비교한다.

## 6. 완료와 전달

- 변경 결과와 이유, 자동 테스트 결과, 브라우저에서 확인한 시나리오와 확인하지 못한 부분을 간단히 전달한다. 실행하지 않은 검증을 통과했다고 보고하지 않는다.
- Bottle Note 제품 이슈의 원본 저장소는 `bottle-note/workspace`다. PR 전 `gh issue view <번호> --repo bottle-note/workspace`로 제목, 범위, 라벨, 완료 조건을 확인한다. 번호를 모르면 그 저장소에서 현재 사용자에게 할당된 프론트엔드 이슈를 찾는다.
- PR에는 이슈를 자동 종료하는 키워드 없이 `[#<번호>](https://github.com/bottle-note/workspace/issues/<번호>)` 형식으로 관련 이슈를 연결한다. 다른 영역의 의존성이 있으면 현재 구현 범위와 후속 작업을 구분한다.
