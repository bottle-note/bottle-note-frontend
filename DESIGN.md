# Bottle Note 디자인 기준

모바일 제품 화면의 공통 기준이다. 구현·테스트는 [AGENTS.md](AGENTS.md)를 따른다.
넓은 화면에서도 아래 모바일 콘텐츠 폭을 유지한다. 별도 레이아웃이 필요한 경우 해당 화면의 요구사항에 따라 설계한다.

## Overview

### 제품

Bottle Note는 주류를 탐색하고 별점·리뷰로 취향을 기록하고 공유하는 모바일 제품이다. 신규 화면도 탐색, 비교, 기록이라는 사용자의 목적에 맞춰 정보와 행동의 우선순위를 정한다.

### 디자인 톤

주류 사진과 사용자의 기록이 중심이 되는, 따뜻하고 차분한 화면을 지향한다. 중립적인 본문과 표면 위에 브랜드색으로 필요한 행동과 선택을 강조한다. 탐색에서는 비교하기 쉬운 정보 정렬, 큐레이션에서는 이미지와 제목, 마이보틀에서는 개인 기록이 먼저 읽히게 한다. MBTI·타로의 특별한 연출은 해당 콘텐츠에 한정한다.

### 신규 화면의 정보 위계

화면을 구성하기 전에 사용자가 여기서 확인할 핵심 정보와 다음 행동을 정한다. 핵심 정보, 판단에 필요한 설명, 부가 정보 순으로 읽히게 하고 제목·묶음·강조 수준을 그 순서에 맞춘다.

- 같은 대상을 설명하는 이름·별점·부연은 하나의 묶음으로 배치한다. 서로 다른 목적의 내용은 섹션으로 나눈다.
- 동일한 역할은 같은 글자·색상·정렬을 반복한다. 중요도를 강조할 때 모든 요소를 동시에 키우거나 진하게 하지 않는다.
- 저장·신청 등 화면의 주요 완료 행동과 취소·추가 정보 등 보조 행동을 구분한다. 같은 행동을 여러 위치에 배치할 때 강조가 경쟁하지 않게 한다.

## Colors

색상은 아래 역할로 선택한다. 정확한 값과 테마 전환은 [색상 토큰](src/style/tokens/semantic-colors.css)을 따른다.

| 사용처                        | 토큰                                                           |
| ----------------------------- | -------------------------------------------------------------- |
| 본문 / 보조 설명              | `fg-neutral` / `fg-neutral-muted`                              |
| 브랜드 / 별점                 | `fg-brand` / `fg-rating`                                       |
| 화면 / 떠 있는 표면 / 앱 바깥 | `bg-layer-default` / `bg-layer-floating` / `bg-layer-basement` |
| 주요 버튼 배경 / 글자         | `bg-brand-solid` / `fg-brand-contrast`                         |
| 입력 경계 / 키보드 포커스     | `stroke-neutral-weak` / `stroke-focus-ring`                    |

표는 역할 이름이다. 사용 시 글자는 `text-`, 배경은 `bg-`, 경계는 `border-`, 포커스 링은 `ring-`을 붙인다.

- 밝은 테마와 어두운 테마에서 같은 정보 위계를 유지한다.
- 사진 위 글자 등 테마와 무관한 표현만 고정 색상을 허용한다.
- 단색 아이콘은 테마색을 따른다. 로고·사진의 색은 유지한다.
- 별점은 테마에 따라 색상이 달라지므로 브랜드색으로 대체하지 않는다. 역할 색상에 임의 투명도를 붙이지 않는다.
- 오류·성공·경고는 같은 상태를 표현하는 기존 컴포넌트를 재사용하고 문구·아이콘을 함께 제공한다.

## Typography

- 모든 화면과 입력창에 **SUIT**를 쓴다.
- 글의 길이가 아닌 **정보의 역할**로 글자 크기와 굵기를 고른다. 같은 역할의 기존 컴포넌트 값을 사용한다.
- 글자 크기는 `text-N`으로 지정한다. `text-15`는 15px이며, 1~100px 범위에서 0.5px 단위를 지원한다. 크기별 토큰을 별도로 추가하지 않는다.
- 기본 줄 높이는 공통 설정을 사용하고 같은 값의 `leading-*`를 중복 지정하지 않는다. 별도 줄 높이가 필요한 기존 콘텐츠는 해당 값을 유지한다.
- 핵심 이름과 정보는 줄바꿈을 허용한다. 말줄임은 전체 내용을 볼 경로가 있을 때만 쓴다.

### 역할별 위계

아래 역할에 맞춰 글자 스타일을 선택한다. 역할 이름을 CSS 클래스명으로 사용하지 않는다.

| 역할      | 판단 기준                                   |
| --------- | ------------------------------------------- |
| 화면 제목 | 현재 화면이나 콘텐츠의 주제를 식별          |
| 섹션 제목 | 독립된 정보 묶음의 주제를 식별              |
| 항목 이름 | 목록·카드에서 비교하거나 선택할 대상을 식별 |
| 본문      | 리뷰·설명 등 사용자가 읽을 핵심 내용        |
| 보조 설명 | 핵심 내용을 이해하는 데 필요한 부연·도움말  |
| 조작 문구 | 버튼·탭·선택 요소가 수행할 행동             |
| 부가 정보 | 날짜·상태 등 내용을 보완하는 짧은 정보      |
| 강조 수치 | 평점·집계 등 해당 영역의 중심 값            |

## Layout

여백·크기·위치의 숫자 클래스는 1px 단위를 사용한다. `mt-16`은 16px, `px-20`은 좌우 20px, `gap-8`은 8px, `h-40`은 40px이다. 0~1024px 범위에서 0.5px 단위를 지원하며, 범위 밖이나 더 정밀한 값은 `w-[1200px]`처럼 지정한다. 숫자형 `leading-20`도 20px이다.

`full`, `auto`, `screen`, `1/2`, `%`, `vh` 등 상대 크기와 안전 영역 계산은 유지한다. `grid-cols-2`의 열 수, `z-10`의 쌓임 순서, `opacity-50`의 투명도는 px 단위가 아니다. 동적 크기는 클래스명을 문자열로 조립하지 않고 완성된 클래스 맵이나 인라인 스타일을 사용한다.

| 대상                | 규격                              | 적용 기준                             |
| ------------------- | --------------------------------- | ------------------------------------- |
| 전체 화면           | 너비 100%, 최대 468px             | 가운데 정렬                           |
| 본문                | 좌우 20px                         | 페이지에서 한 번만 적용               |
| 전체 폭 이미지·배너 | 좌우 0px                          | 본문과 별도 영역                      |
| 컴포넌트 내부       | 아래 유형별 규격                  | 본문 여백과 구분                      |
| 고정 영역           | 실제 점유 높이만큼 본문 공간 확보 | 노치·홈 표시줄 포함, 키보드 겹침 확인 |
| 스크롤              | 페이지 전체                       | 모달·시트는 필요할 때 분리            |

### 간격

신규 화면에는 다음 간격을 기본으로 적용한다.

| 관계                       | 간격 | 사용 예                         |
| -------------------------- | ---- | ------------------------------- |
| 한 정보의 세부 요소        | 4px  | 정보명과 짧은 부연, 태그 사이   |
| 관련 정보                  | 8px  | 주류 이름·영문명·평가 정보      |
| 이미지와 설명              | 12px | 탐색 목록의 병 이미지와 본문    |
| 제목과 내용·일반 카드 사이 | 16px | 섹션 제목과 목록, 홈 가로 카드  |
| 독립된 정보 묶음           | 32px | 설정 내용과 별도 안내·설정 영역 |

간격은 요소 사이의 최종 거리다. 양쪽에 중복 적용하지 않는다. 내부 패딩·줄 높이는 별개이며, 기존 컴포넌트를 일괄 치환하지 않는다.

### 화면 조합

| 화면의 목적 | 읽는 순서와 구성                              | 우선할 패턴                                            |
| ----------- | --------------------------------------------- | ------------------------------------------------------ |
| 탐색·비교   | 검색·조건 → 결과 → 대상의 이름·평점·부연      | 동일한 정보 위치를 반복하는 목록 행                    |
| 콘텐츠 탐색 | 이미지·제목 → 짧은 설명 → 상세 이동           | 커버 카드; 상세에서는 피드 높이를 재사용하지 않음      |
| 상세 확인   | 대상 식별 → 핵심 정보 → 관련 설명·기록 → 행동 | 섹션으로 나눈 본문; 관련 정보만 카드로 묶음            |
| 입력·기록   | 대상·입력 목적 → 입력 항목·도움말 → 제출      | 입력과 관련 오류를 가까이 배치하고 하단 행동 공간 확보 |

목록은 항목 간 비교가 중요할 때, 카드는 하나의 독립된 콘텐츠나 정보 묶음을 구분할 때 선택한다. 여백으로 충분히 구분되는 본문까지 모두 카드로 감싸지 않는다. 표면·테두리·그림자는 구분할 이유가 있을 때 사용한다.

## Elevation & Depth

화면, 떠 있는 표면, 앱 바깥은 Colors의 표면 역할로 구분한다. 고정 헤더·하단 행동은 본문을 가리지 않게 공간을 확보한다. 모달·시트는 배경 조작을 막고 닫은 뒤 원래 위치로 포커스를 돌린다.

그림자는 같은 역할의 기존 컴포넌트 표현을 따른다. 새 겹침 영역은 관련 헤더·버튼·모달의 z-index를 확인해 배치하고, 모달·시트와 배경 차단 영역이 고정 헤더·버튼에 가려지지 않게 한다.

## Shapes

기존 컴포넌트의 크기·간격·모서리를 유지한다. 재배치를 이유로 바꾸지 않는다. 모서리는 아래 유형별 규격을 따르며 모든 요소에 같은 값을 일괄 적용하지 않는다.

병 이미지는 병과 라벨이 잘리지 않게, 커버·배너는 영역을 채우게 표시한다. 폴더 탭은 기존 전용 윤곽을 사용한다. 단색 아이콘은 기존 인접 컴포넌트의 형태와 크기를 따른다. 작은 아이콘은 시각적 크기를 유지하면서 주변 클릭 영역을 확보한다.

## Components

### 규격 적용

단위는 px, 너비는 별도 표기가 없으면 부모 영역을 채운다. ‘자동’은 내용에 따른 크기다. 유형별 값은 위 공통 간격보다 우선하며, 특정 화면의 예외를 다른 유형에 확대하지 않는다.

### 헤더

| 유형·사용처             | 높이                                   | 내부 여백·배치                                              |
| ----------------------- | -------------------------------------- | ----------------------------------------------------------- |
| 뒤로 가기형 · 상세/설정 | 자동                                   | 좌우 17, 위 20 + 상단 안전 여백, 아래 15; 좌·중·우 3영역    |
| 로고형 · 홈/프로필      | 자동                                   | 뒤로 가기형과 동일; 우측 아이콘 사이 12; 스크롤에 따라 숨김 |
| 탭형 · 탐색/큐레이션    | 펼침 91, 접힘 52 + 각각 상단 안전 여백 | 로고 + 폴더 탭; 검색 영역은 별도 높이                       |

### 검색·탭·필터

| 유형·사용처                    | 크기                    | 내부 여백·간격                                       | 모서리       |
| ------------------------------ | ----------------------- | ---------------------------------------------------- | ------------ |
| 테두리 검색창 · 검색 전용 화면 | 높이 40                 | 왼쪽 16, 오른쪽 48                                   | 8            |
| 밑줄 검색창 · 탭형 목록        | 자동                    | 좌우 8, 상하 10; 하단 선 2; 우측 행동 공간 별도 확보 | 0            |
| 밑줄 탭 · 영역 전환            | 높이 자동, 항목 균등 폭 | 아래 8, 탭 사이 12                                   | 0            |
| 폴더 탭 · 상위 목록 전환       | 항목 146 × 32           | 첫 항목 왼쪽 16, 다음 항목 12 겹침                   | 전용 탭 윤곽 |
| 검색 필터 버튼                 | 아이콘 20 × 20          | 아이콘 크기와 별도로 주변 클릭 영역 확보             | 4            |
| 선택 태그 · 조건 선택          | 자동                    | 좌우 12, 상하 4; 아이콘과 글자 사이 4                | 6            |
| 정보 배지 · 상태 표시 기본형   | 자동                    | 좌우 10, 상하 4; 아이콘과 글자 사이 4                | 6            |

### 목록·카드

| 유형·사용처              | 높이 | 내부 여백·간격                                                          | 모서리    |
| ------------------------ | ---- | ----------------------------------------------------------------------- | --------- |
| 주류 목록 행 · 탐색      | 자동 | 상하 24; 이미지↔본문 12, 정보 사이 8                                   | 없음      |
| 리뷰 피드 · 탐색         | 자동 | 위 30; 작성자 영역 뒤 20; 이미지·본문·반응 사이 14                      | 외곽 없음 |
| 리뷰 요약 행 · 주류 상세 | 자동 | 상하 35; 정보 사이 10; 본문↔썸네일 8                                   | 외곽 없음 |
| 리뷰 기록 행 · 마이보틀  | 자동 | 상하 16; 이미지↔본문 12, 정보 사이 6                                   | 외곽 없음 |
| 큐레이션 커버 카드       | 157  | 내부 16; 배지↔제목 16, 제목↔설명 8                                    | 8         |
| 프로그램 커버 카드       | 248  | 내부 16; 배지↔제목 12, 제목↔설명 8                                    | 8         |
| 시음회 커버 카드         | 390  | 좌우 16, 상하 20; 배지↔제목 16, 제목↔설명 14                          | 8         |
| 행사 정보 카드           | 자동 | 내부 16; 행 사이 16, 여러 줄 표시형 24; 아이콘↔본문 10, 정보명↔부연 4 | 16        |

큐레이션 피드 카드 사이는 28이다. 커버 높이는 해당 피드 전용이며 상세 화면에 적용하지 않는다.

### 입력·버튼

| 유형·사용처                | 크기             | 내부 여백·간격                   | 모서리 |
| -------------------------- | ---------------- | -------------------------------- | ------ |
| 밑줄 입력 · 닉네임         | 높이 자동        | 상하 8                           | 0      |
| 금액 입력 · 리뷰 가격      | 높이 28          | 입력 오른쪽 8; 단위는 별도 영역  | 0      |
| 본문 입력 · 리뷰 작성      | 높이 192         | 내부 12; 아래 글자 수 표시까지 4 | 8      |
| 기본 버튼 · 제출/확인      | 높이 52          | 문구 가운데 정렬                 | 12     |
| 두 버튼 · 취소/확인        | 높이 52, 균등 폭 | 버튼 사이 8                      | 12     |
| 하단 고정 버튼 · 신청/저장 | 높이 52          | 화면 좌우 20                     | 12     |

일반 행동 버튼은 `src/components/ui/Button/Button.tsx`의 `Button`을 사용한다. 높이·패딩·글자·모서리·상태색을 사용처에서 직접 지정하지 않는다. `className`은 너비·바깥 여백 등 배치에 사용한다.

| 크기 | 높이 | 사용 상황                                             |
| ---- | ---- | ----------------------------------------------------- |
| `sm` | 28px | 태그 추출·등록, 팔로우, 닉네임 변경 등 짧은 보조 행동 |
| `md` | 40px | 장소 검색, 콘텐츠 내 재시도·로그인 등 독립된 행동     |
| `lg` | 52px | 화면 하단의 저장·등록·신청, 모달 확인·취소            |

| 강조 방식   | 표현                         | 사용 상황                   |
| ----------- | ---------------------------- | --------------------------- |
| `primary`   | 코랄 배경·흰 글자            | 현재 작업의 주요 행동       |
| `secondary` | 기본 표면·코랄 테두리와 글자 | 취소·태그 추출 등 보조 행동 |
| `text`      | 채움·테두리 없는 중립색 글자 | 수정하기·부가 행동          |

크기와 강조 방식은 독립적으로 선택한다. 리뷰 등록은 `lg + primary`, 모달 취소는 `lg + secondary`, 태그 추출은 `sm + secondary`를 사용한다. 코랄 채움은 핵심 행동에 제한한다.

기본값은 `lg + primary`다. `lg`는 부모 폭을 채우며, `sm`·`md`는 내용에 맞춘다. 폭을 채워야 하는 경우 `fullWidth`를 지정한다. 문구는 `children`으로 전달하고, 기존 호출의 `btnName`도 지원한다. 비활성은 `disabled`로 전달한다.

```tsx
<Button size="sm" variant="secondary" onClick={handleExtractTags}>
  태그 추출
</Button>
<Button size="md" variant="secondary" fullWidth onClick={handleSearch}>
  장소 검색
</Button>
```

두 버튼은 `DualButton`, 하단 고정 행동은 `StickyBottomCta`로 조합한다. 이동용 링크는 `<a>` 또는 `<Link>`의 의미를 유지하고 `buttonVariants`로 동일한 표현을 적용한다. 탭·선택 칩·아이콘 전용 행동·이미지 선택·카드 링크는 각각의 컴포넌트 규격을 따른다.

### 하단 메뉴·모달·패널

| 유형·사용처                | 크기                                                | 내부 여백·배치                                             | 모서리  |
| -------------------------- | --------------------------------------------------- | ---------------------------------------------------------- | ------- |
| 하단 메뉴                  | 높이 70                                             | 화면 좌우 16; 내부 좌우 26, 상하 16; 아이콘↔글자 2        | 13      |
| 확인·알림 모달             | 최대 화면 폭 468 안에서 좌우 16 제외, 최소 높이 208 | 내부 좌우 16, 위 64, 아래 16; 주요 블록 사이 12            | 12      |
| 하단 시트 · 맥락 내 선택   | 최대 폭 468, 기본 높이 화면의 80%                   | 손잡이 48 × 6, 위 16; 본문 패딩은 내용별                   | 위쪽 16 |
| 필터 패널 · 여러 조건 선택 | 너비 288, 화면 전체 높이                            | 화면 오른쪽; 위 64, 헤더 좌우 20·아래 12; 본문 독립 스크롤 | 0       |

하단 메뉴·고정 버튼의 아래 여백은 **24 또는 하단 안전 여백 + 8 중 큰 값**이다. 둘을 함께 표시하면 별도 배치한다. 시트 높이는 내용에 맞게 조절한다. 패널은 안전 여백을 확보하고 화면 폭이 468px보다 넓을 때도 앱 콘텐츠 영역에 맞춰 정렬한다.

### 선택 기준

- 탭은 동등한 영역 전환, 필터는 현재 결과를 좁히는 조건 선택에 사용한다. 정보 배지는 클릭 가능한 선택 태그와 구분한다.
- 입력에는 입력 목적을 식별할 이름을 제공하고, 도움말·오류를 해당 입력과 가까이 둔다. placeholder만으로 이름을 대신하지 않는다.
- 확인 모달은 즉시 판단할 확인·알림에, 하단 시트는 현재 맥락에서의 선택에 사용한다. 여러 조건을 함께 다루는 필터는 기존 패널 패턴을 참조한다.
- 하단 메뉴는 화면 간 이동, 하단 고정 버튼은 현재 화면의 행동을 담당한다. 두 요소가 동시에 필요한 경우 각각의 자리와 본문 공간을 설계한다.

표에 없는 체크박스·라디오·스위치, 선택형 입력, 알림·로딩·빈 상태·오류 표현은 같은 역할의 기존 컴포넌트를 찾아 재사용한다. 기존 패턴으로 요구사항을 해결할 수 없으면 필요한 차이와 적용 범위를 명시한다.

## Do's and Don'ts

### 콘텐츠

- 병 이미지는 병과 라벨이 잘리지 않게, 커버·배너는 영역을 채우게 표시한다.
- 사진 위 글자는 밝은 사진에서도 읽혀야 한다.
- 한글 주명과 영문 보조명, 내 별점과 전체 평점을 구분한다.
- 미평가를 0점으로, 가격 미정을 무료로 표시하지 않는다.
- 리뷰의 공개 범위·가격 단위 등 제품 의미는 디자인 변경으로 바꾸지 않는다.

### 행동과 상태

- 주요 행동과 보조 행동, 정보 배지와 선택 버튼을 구분한다.
- 로딩·빈 목록·검색 결과 없음·오류·로그인 필요 상태를 구분하고 다음 행동을 안내한다.
- 추가 로딩 중에도 기존 목록은 유지한다.
- 선택·오류는 색상만으로 알리지 않는다. 제출 중·비활성·키보드 포커스도 표시한다.
- 작은 아이콘도 쉽게 누를 수 있게 한다. 긴 이름·많은 태그·이미지 실패를 고려한다.
- 모달·시트는 닫기 방법을 제공하고 배경 조작을 막는다. 닫으면 원래 위치로 포커스를 돌린다.
- 문구는 할 일과 결과가 드러나게 짧게 쓴다.

### 확인과 참조

문서에 없는 규격은 같은 역할의 기존 컴포넌트와 호출부를 확인해 적용한다.
구현 후 좁은 화면·밝은/어두운 테마·스크롤·입력 상태를 확인한다.

- 색상: [의미별 토큰](src/style/tokens/semantic-colors.css), [팔레트](src/style/tokens/colors.css)
- 크기·간격: [Tailwind 설정](tailwind.config.ts)
- 화면 폭·고정 영역: [전역 스타일](src/style/globals.css)
- 현재 구현: [UI 조사](docs/design/current-ui-audit.md), [페이지별 여백](docs/design/page-layout-ownership.md)
- 미확정 사항·구현 주의점: [검토 메모](docs/design/design-review-notes.md)

기존 코드가 이 기준과 다르면 차이를 알린다. 기존 구현 자체를 새 표준으로 삼지 않는다.
